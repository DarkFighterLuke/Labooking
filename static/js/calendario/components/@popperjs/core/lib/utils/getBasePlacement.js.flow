// @flow
import {auto, type BasePlacement, type Placement} from '../enums';

export default function getBasePlacement(
    placement: Placement | typeof auto
): BasePlacement {
  return (placement.split('-')[0]: any);
}
